function getRandomInt(max) {
    return Math.floor(Math.random() * max);
}

// document.addEventListener('DOMContentLoaded', () => {
//     let preloadDIVs = document.querySelectorAll('.ldio-yzaezf3dcmj div');

//     preloadDIVs.forEach((div) => {
//         div.addEventListener('animationiteration', () => {
//             div.style.borderRadius = `${getRandomInt(60)}% ${getRandomInt(60)}% ${getRandomInt(60)}% ${getRandomInt(60)}% / 
//                 ${getRandomInt(60)}% ${getRandomInt(60)}% ${getRandomInt(60)}% ${getRandomInt(60)}%`;

//             console.log("borderRadius змінено після повтору анімації!");
//         });
//     });
// });
